<section class="container">



<%= form_with(model: post_admin, local: true) do |form| %>
  <% if post_admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post_admin.errors.count, "error") %> prohibited this post_admin from being saved:</h2>

      <ul>
      <% post_admin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, class: "form-control"  %>
  </div>

  <div class="field">
    <%= form.label :subtitle %>
    <%= form.text_field :subtitle, class: "form-control"  %>
  </div>

  <div class="field">
    <%= form.label :topic %>
    <%= form.text_field :topic, class: "form-control"  %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, class: "form-control post-content"  %>
  </div>

  <div class="field">
    <%= form.label :image_url %>
    <%= form.text_field :image_url, class: "form-control"  %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

</section>
